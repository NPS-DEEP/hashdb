# NOTE: This Makefile does not define a primary, so source files are
# manually included using EXTRA_DIST.

# NOTE: To build win32, win64, or windist, use hashdb/Makefile.

EXTRA_DIST = \
	CONFIGURE_F18.bash \
	icu-mingw32-libprefix.patch \
	icu-mingw64-libprefix.patch \
	zmq-configure.in.patch \
	zmq-configure.patch \
	zmq-zmq.h.patch \
	zmq-zmq_utils.h.patch

