To cross-compile Windows executables and build the Windows installer:

1) Run script CONFIGURE_F18.sh to set up Fedora 18 or newer with the
mingw environment required for cross-compiling to Windows.

2) type "make" to build the Windows installer.

